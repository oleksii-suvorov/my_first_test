Main goal was to simulate an API connection - get and use data from users.json file with some very simple routing, 
styling and other simple functionality (ordering, updating and deleting data).
But deleting func only from the state array, not from server.
